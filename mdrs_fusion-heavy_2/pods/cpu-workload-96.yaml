apiVersion: v1
kind: Pod
metadata:
  name: cpu-workload-96
  labels:
    app: cpu-workload
    workload-type: cpu
    run-id: "mdrs_fusion-heavy_2"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: cpu-app
    image: fathiimran/workload-cpu:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "280m"
        memory: "512Mi"
      limits:
        cpu: "280m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
