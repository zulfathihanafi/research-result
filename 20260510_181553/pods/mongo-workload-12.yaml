apiVersion: v1
kind: Pod
metadata:
  name: mongo-workload-12
  labels:
    app: mongo-workload
    workload-type: mongo
    run-id: "20260510_181553"
    scheduler: "default-scheduler"
spec:
  schedulerName: default-scheduler
  restartPolicy: Never
  containers:
  - name: mongo-app
    image: fathiimran/workload-mongo:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "293m"
        memory: "512Mi"
      limits:
        cpu: "293m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
