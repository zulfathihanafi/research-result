apiVersion: v1
kind: Pod
metadata:
  name: fusion-workload-25
  labels:
    app: fusion-workload
    workload-type: fusion
    run-id: "20260512_024505"
    scheduler: "default-scheduler"
spec:
  schedulerName: default-scheduler
  restartPolicy: Never
  containers:
  - name: fusion-app
    image: fathiimran/workload-fusion:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "365m"
        memory: "512Mi"
      limits:
        cpu: "365m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
