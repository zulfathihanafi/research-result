apiVersion: v1
kind: Pod
metadata:
  name: fusion-workload-92
  labels:
    app: fusion-workload
    workload-type: fusion
    run-id: "drs_fusion-heavy_1"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: fusion-app
    image: fathiimran/workload-fusion:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "498m"
        memory: "512Mi"
      limits:
        cpu: "498m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
