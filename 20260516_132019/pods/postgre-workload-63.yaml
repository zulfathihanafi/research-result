apiVersion: v1
kind: Pod
metadata:
  name: postgre-workload-63
  labels:
    app: postgre-workload
    workload-type: postgre
    run-id: "20260516_132019"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: postgre-app
    image: fathiimran/workload-postgre:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "466m"
        memory: "512Mi"
      limits:
        cpu: "466m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
