apiVersion: v1
kind: Pod
metadata:
  name: fusion-workload-14
  labels:
    app: fusion-workload
    workload-type: fusion
    run-id: "20260518_040716"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: fusion-app
    image: fathiimran/workload-fusion:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "264m"
        memory: "512Mi"
      limits:
        cpu: "264m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
