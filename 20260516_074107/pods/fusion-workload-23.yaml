apiVersion: v1
kind: Pod
metadata:
  name: fusion-workload-23
  labels:
    app: fusion-workload
    workload-type: fusion
    run-id: "20260516_074107"
    scheduler: "default-scheduler"
spec:
  schedulerName: default-scheduler
  restartPolicy: Never
  containers:
  - name: fusion-app
    image: fathiimran/workload-fusion:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "431m"
        memory: "512Mi"
      limits:
        cpu: "431m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
