apiVersion: v1
kind: Pod
metadata:
  name: mongo-workload-13
  labels:
    app: mongo-workload
    workload-type: mongo
    run-id: "kube_fusion-heavy_1"
    scheduler: "default-scheduler"
spec:
  schedulerName: default-scheduler
  restartPolicy: Never
  containers:
  - name: mongo-app
    image: fathiimran/workload-mongo:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "453m"
        memory: "512Mi"
      limits:
        cpu: "453m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
