apiVersion: v1
kind: Pod
metadata:
  name: mongo-workload-69
  labels:
    app: mongo-workload
    workload-type: mongo
    run-id: "mdrs_fusion-heavy_1"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: mongo-app
    image: fathiimran/workload-mongo:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "285m"
        memory: "512Mi"
      limits:
        cpu: "285m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
