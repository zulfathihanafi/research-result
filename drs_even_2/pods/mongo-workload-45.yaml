apiVersion: v1
kind: Pod
metadata:
  name: mongo-workload-45
  labels:
    app: mongo-workload
    workload-type: mongo
    run-id: "drs_even_2"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: mongo-app
    image: fathiimran/workload-mongo:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "381m"
        memory: "512Mi"
      limits:
        cpu: "381m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
