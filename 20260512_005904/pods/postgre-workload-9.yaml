apiVersion: v1
kind: Pod
metadata:
  name: postgre-workload-9
  labels:
    app: postgre-workload
    workload-type: postgre
    run-id: "20260512_005904"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: postgre-app
    image: fathiimran/workload-postgre:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "294m"
        memory: "512Mi"
      limits:
        cpu: "294m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
