apiVersion: v1
kind: Pod
metadata:
  name: mongo-workload-2
  labels:
    app: mongo-workload
    workload-type: mongo
    run-id: "20260510_152946"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: mongo-app
    image: fathiimran/workload-mongo:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "495m"
        memory: "512Mi"
      limits:
        cpu: "495m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
