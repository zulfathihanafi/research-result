apiVersion: v1
kind: Pod
metadata:
  name: fusion-workload-173
  labels:
    app: fusion-workload
    workload-type: fusion
    run-id: "mdrs_fusion-only_1"
    scheduler: "dqn-scheduler"
spec:
  schedulerName: dqn-scheduler
  restartPolicy: Never
  containers:
  - name: fusion-app
    image: fathiimran/workload-fusion:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "270m"
        memory: "512Mi"
      limits:
        cpu: "270m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
