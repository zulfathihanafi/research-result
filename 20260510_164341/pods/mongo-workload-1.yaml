apiVersion: v1
kind: Pod
metadata:
  name: mongo-workload-1
  labels:
    app: mongo-workload
    workload-type: mongo
    run-id: "20260510_164341"
    scheduler: "default-scheduler"
spec:
  schedulerName: default-scheduler
  restartPolicy: Never
  containers:
  - name: mongo-app
    image: fathiimran/workload-mongo:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "284m"
        memory: "512Mi"
      limits:
        cpu: "284m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
