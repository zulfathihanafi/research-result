apiVersion: v1
kind: Pod
metadata:
  name: postgre-workload-1
  labels:
    app: postgre-workload
    workload-type: postgre
    run-id: "20260510_152452"
    scheduler: "drs-scheduler"
spec:
  schedulerName: drs-scheduler
  restartPolicy: Never
  containers:
  - name: postgre-app
    image: fathiimran/workload-postgre:1.2
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: "380m"
        memory: "512Mi"
      limits:
        cpu: "380m"
        memory: "512Mi"
    envFrom:
    - configMapRef:
        name: db-workload-config
